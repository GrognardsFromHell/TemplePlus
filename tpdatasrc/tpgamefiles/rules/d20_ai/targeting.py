import tpai

def consider_target(obj, tgt):
    return None # this causes the engine to ignore the result; return 0 / 1 to make it work

def execute_strategy(obj, target):

    return None # this causes the engine to ignore the result; return 0 / 1 to make it work